# 顧客修正依頼27項目 監査結果（2026-07-04 実施, 34エージェント/17分, 反証検証済み）

復元元: workflow lec-audit-27-items wf_4936792b。監査対象=作業ツリー index.html（未コミットv18.26含む）。

## implemented (12件): 項目1, 項目2, 項目4, 項目5, 項目6, 項目17, 項目19, 項目21, 項目22, 項目24, 項目25, 項目26
## partial (12件): 項目3, 項目9, 項目10, 項目11, 項目12, 項目14, 項目15, 項目16, 項目18, 項目20, 項目23, 項目27
## missing (3件): 項目7, 項目8, 項目13

---

### 項目1 [implemented] 右下の同時利用中アナウンスバーを開閉（最小化）できる
**根拠**: /Users/takaishouhei/Claude案件/株式会社LEC/在庫管理システム/index.html — 2986行: "var presenceMin=us(localStorage.getItem('lec_presence_min')==='1');/* v18.21 右下同時利用バーの最小化状態 */"（初期状態復元）。3314行: "h('button',{className:'pf-toggle',title:'最小化',onClick:function(){presenceMin[1](true);try{localStorage.setItem('lec_presence_min','1');}catch(_){}}},'▾')"（展開時の最小化ボタン）。3309行: "h('div',{className:'presence-floating collapsed'...,title:'クリックで同時利用バーを開く',onClick:function(){presenceMin[1](false);try{localStorage.setItem('lec_presence_min','0');}..."（最小化時はバー全体クリックで再展開、👥人数ピル+▴表示は3310-3312行）。CSS 52-58行: ".presence-floating.collapsed{cursor:pointer;padding:8px 14px;...}" ".pf-toggle{position:absolute;...cursor:pointer...}"。マウント確認: 2976行 "function App(){"、3270行 "return h('div',null,"（ルートdivにonClickなし=干渉なし）、7854行 "ReactDOM.createRoot(document.getElementById('app')).render(h(App));"。スクリプト全体はnode --checkでSYNTAX OK。本番HEAD(13bcd00)にも同実装（HEAD側2832/3155/3160行）。修正まとめ_20260615.html 138行「右下『同時利用中』バーを開閉できるように」・142行「最小化ボタン（▾）…クリックで元に戻ります…状態はブラウザに記憶」。
**不足/差分**: なし。軽微な観察のみ: (1) モバイル幅では @media（92行 .presence-floating{left:8px;right:8px;...}）により最小化ピルも横幅いっぱいに広がるが、高さ・情報量は縮小されるため「開閉できる」要望は満たす。(2) offline時はバー自体が非表示（3308行の条件）だがこれはv14.4からの意図的仕様であり本要望の欠陥ではない。

### 項目2 [implemented] 誰が編集権限を保有しているかが画面上で分かる
**根拠**: 2290行: function lockEditorName(users){...var pk=...SYSTEM_LOCK.primary...users.find(function(u){return u.presenceKey===pk;});return e?e.name:'';} ／ 3022行: users.push({name:p.name,...,presenceKey:k}) と 3028-3029行: var res=resolveEditor(allKeys,users,SYSTEM_LOCK.overrideName,...); SYSTEM_LOCK.primary=res.primaryKey; で表示用データとロック判定が同一ソース ／ 3277行: '🔒 読み取り専用モード（編集中: '+(lockEditorName(onlineUsers[0])||'別のユーザー')+'）' ／ 3321行: '🔒 読み取り専用'+(lockEditorName(onlineUsers[0])?'（編集中: '+lockEditorName(onlineUsers[0])+'）':'') : '✓ 編集権限' ／ 3361行: '現在の編集者: ',h('b',null,lockEditorName(onlineUsers[0])||editorOverrideName[0]||'（先勝ち＝最初にログインした人）') ／ 3030行: var newRO=!res.amIPrimary&&allKeys.length>1; isReadOnly[1](newRO); onlineUsers[1](users);（sync時に再計算・再描画） ／ 3164-3166行: override受信時に '🔑 …が編集権限を「…」に付与しました' 通知＋recomputeLock。対象ファイル: /Users/takaishouhei/Claude案件/株式会社LEC/在庫管理システム/index.html
**不足/差分**: 判定を覆さない軽微な限界が2点: (1) 営業ロール(sales)ユーザーには編集者名が出ない。3277行・3321行とも roleReadOnly の分岐が isReadOnly より先に評価され「🔒 閲覧専用（営業ロール）」のみ表示、かつモーダルの「現在の編集者:」(3361行)は isAdmin ブロック(3359行)内のため非管理者には非表示。ただし営業は恒久閲覧専用でロック対象外のため要望の主旨(先勝ちロックで読めなくなった人が誰にブロックされているか分かる)は満たしている。(2) 編集者本人向けの「✓ 編集権限」表示はフローティングバー展開時のみで、最小化時(3309-3312行)は人数のみ（赤バナー非表示で編集可と判別は可能）。

### 項目3 [partial] 営業ロールは数値変更も仮押さえも不可（閲覧のみ）、事務ロールは全体的に操作可
**根拠**: 実装済み部分: 3266-3268行 "var currentRole=roleOf(userName[0]); var roleReadOnly=currentRole==='sales'; var effRO=isReadOnly[0]||roleReadOnly;"、3301-3304行で全ページに isReadOnly:effRO を伝搬、3812/3857/3918行 "if(p.isReadOnly){p.toast('読み取り専用モード中は仮押さえできません');return;}"、4308-4309行 "p.salesView?null:h('button',...,'仮押さえのみ（見積無）')"、3886-3892行 営業はプレビューのみ（no:'(プレビュー)' でORDERS非保存・非採番）、5078行 在庫編集ガード、4587行 changeStatus ガード。／ガード漏れ（反証）: 7078行 "h('td',null,inc.status!=='入庫済'?h('button',{className:'btn btn-xs btn-gn',onClick:function(){"（入庫処理・ガード無し）→7092行 "STK[inLine][inc.wh][it.key]=(STK[inLine][inc.wh][it.key]||0)+it.qty;"→7095行 "inc.status='入庫済';bump[1](bump[0]+1);p.toast('入庫処理完了: INC-'+inc.id);saveAll();"。7107-7112行 追加ボタン "INCOMING.push({id:nextIncId++,...,status:'予定'});"（ガード無し）。6275行 "onChange:function(e){op.order.shipPrintedDate=e.target.value;saveAll();bump[1](bump[0]+1);}" / 6277行 shipSentDate 同様（ガード無し・出荷済み一覧サブタブ 6149行）。7176-7184行 単価マスタ "UNIT_COST[row.key][row.sub]=v ... UNIT_COST[row.key]=v"（ガード無し・非永続）。5248行 "h('button',...onClick:function(){showIncoming[1](true);}},'入庫予定')"（無条件表示で営業も到達可）。2442行 "setInterval(saveAll,3000);"（未ガード変更が3秒毎にlocalStorage永続化）。
**不足/差分**: (1) 在庫表ページ→入庫予定モーダルの「入庫処理」ボタン(7078-7096行)に p.isReadOnly ガードが無く、営業が STK を直接増加できる（在庫数値変更の穴・最重要）。(2) 「＋入庫予定を追加」(7107-7115行)に同ガードが無く、営業が INCOMING を追加できる（(1)と組み合わせると営業単独で在庫増加が完結）。(3) 出荷済み一覧の「指示書作成日」「送付日」入力(6275/6277行)がガード・disabled 無しで案件データを書き換え saveAll する。(4) 単価マスタ(7176-7186行)の仕入単価が営業でも編集可（セッション内のみだが在庫資産計算に影響）。修正は各ハンドラ冒頭に if(p.isReadOnly){p.toast(...);return;} を追加、または入力/ボタンに disabled:p.isReadOnly を付与する数行の差分で足りる。

### 項目4 [implemented] 読み取り専用モードでも「在庫確認」「明細追加」ボタンは押せる
**根拠**: 3631-3648行: function addLine(){ if(!cust)return; …lines[1](nl);doCheck(nl);} にROガードなし／3649-3669行: addPartLine 同様ガードなし／3672-3678行: addFreeInputLine 同様ガードなし／4101行: h('button',{className:'btn btn-nv',style:{width:'100%'},onClick:addLine},'＋ 在庫確認・明細追加')（disabledなし）／4134行: onClick:addPartLine、4151行: onClick:addFreeInputLine（いずれもdisabledなし）／grep全件確認: disabled:p.isReadOnly は5364行(在庫表取込の反映ボタン)のみ、pointer-eventsはheld-edit-cell装飾(66/68行)のみでROオーバーレイなし／3497行: h('button',{className:'btn btn-xs btn-nv',onClick:function(){p.selectCust(c);}},'選択 →')＝RO時も顧客選択可（ブロックは3449/3461/3468行の追加・編集・削除のみ）／3273-3275行: タブボタンにRO無効化なし／2123行〜 checkAllLinesWH はローカル集計のみでSTK/HELD書込なし／3886-3893行: lockReadOnly/salesViewは見積プレビューのみ、3918行: if(p.isReadOnly){p.toast('読み取り専用モード中は仮押さえできません');return;} で保存系ブロック維持／修正まとめ_20260616.html 125-128行: 「読み取りモードでも『在庫確認』は全員できるように…全員いつでも可能に」
**不足/差分**: なし。軽微な注記として、監査リストの「項目4」はコード内タグ・顧客報告上は「v18.25 要望⑥」に対応する（採番のズレのみで内容は一致）。

### 項目5 [implemented] スマホライクなUI（営業が外出先でスマホから在庫チェックできる）
**根拠**: 83-94: 「/* v18.14 P6(要望5): スマホ向けタッチUI */ @media(max-width:768px){ .hdr{height:auto;...flex-wrap:wrap} .tabs{overflow-x:auto;-webkit-overflow-scrolling:touch} .tab{padding:9px 14px;font-size:14px} .btn{min-height:38px} .inp{min-height:36px;font-size:16px}/* 16px未満だとiOSが入力時にズームする */ .page table{display:block;overflow-x:auto;white-space:nowrap} }」／3555: 「var isMobile=us(typeof window!=='undefined'&&window.innerWidth<=768);」＋3558: resizeリスナーで動的更新／3975: 「flexDirection:isMobile[0]?'column':'row'」で在庫確認・見積の左右2カラムをスマホで縦積み、3977・4256: 「width:isMobile[0]?'100%':320」「flex:1,width:isMobile[0]?'100%':'auto'」で商品選択・倉庫別在庫を全幅化。viewport metaも4行目に設定済み。UI描画（メディアクエリ＋条件スタイル）とハンドラ（resizeイベント）の両方が実際に効くことを確認。
**不足/差分**: なし（軽微な残課題: ログイン画面カードがwidth:400固定=3229付近で幅400px未満の端末では僅かに横スクロール。ヘッダ右の「ユーザー管理/切替」ボタンはインラインstyle fontSize:7のため .btn の min-height:38px 対象外でタップターゲットが小さい）

### 項目6 [implemented] アカウント3つ（吉野=管理者/事務/営業）。吉野がID/PWを発行できる。誰がログインしているか常時表示
**根拠**: 2276-2282: 「/* v18.24 要望6: 初期アカウントを3つ(管理者/営業/事務)に */ var USERS=[{id:'admin',name:'管理者',...role:'admin'},{id:'u_sales',name:'営業',...role:'sales'},{id:'u_jimu',name:'事務',...role:'user'}];」／発行UI: 3405: 「🆕 アカウント発行（新規ユーザー）」（3407: if(!isAdmin)return null で管理者のみ表示）、3413-3414: ハンドラ内でも「currentUser2.role!=='admin'→'管理者のみ発行可能です'」を再チェック、3417-3423: hashPwでPW保存→USERS.push→saveUsers()+cloudSave()で全端末同期→lastIssued_に発行情報格納／3340-3346: 発行結果パネル「【LEC在庫管理システム ログイン情報】ログインID/パスワード/権限/URL」＋📋コピー ボタン／3384-3390: 既存ユーザーの🔑PW設定・変更・解除（管理者のみ）／3216: doLoginで「if(hashPw(loginPw[0])!==u.pw){showToast('パスワードが違います');return;}」と実照合／常時表示: 3290: sticky ヘッダ内に「h('span',...,myLocLabel+': '+userName[0])」、3282-3288: online-barで同時ログイン中ユーザー一覧＋人数を常時表示。
**不足/差分**: なし（留意: フォールバック初期アカウント名は「管理者/営業/事務」でありコード上に「吉野」個人名は無い＝実運用の名前とPWはクラウド lec_state.users で管理する設計（2276-2277コメント）。PW未設定ユーザーは空欄でログイン可（ロックアウト防止仕様）。hashPwはクライアント側の簡易ハッシュでソフトアクセス制御。ヘッダのログイン者名はfontSize:8と小さい）

### 項目7 [missing] 商品選択に営業向けシンプル入力（台数とベース）パターンを追加し、既存詳細パターンと2パターン化
**根拠**: index.html 3979: h('div',{className:'chd'},h('h3',null,'商品選択')) — 入力フォームはこの1カードのみでモード切替なし / 3629: var qty=(parseInt(isl[0])||1)*(parseInt(tier[0])||1); — 台数は島数×段数の導出のみ / 4060-4062: '島数（列）'と'段数'の2入力（台数直接入力欄なし） / 4064-4066: h('b',{...},fm(qty)+'台') — 台数は表示のみ / 4101: h('button',{...,onClick:addLine},'＋ 在庫確認・明細追加') — 追加ボタンは1個 / 3631-3648: function addLine(){...} — 詳細フォームstate(isl/tier/lk/doorType/hasBase等)前提の単一経路 / 3575: var baseColor=us('gy'),hasBase=us(true) — ベースは詳細フォーム内のチェックボックス(4073-4077) / 4139: h('h3',null,'自由入力（在庫外商品）') — 名称/数量/単価の在庫外入力で台数＋ベースのBOM展開パターンではない / 7544: /* クイックプリセット */ — 在庫増減期間Excelモーダルの日付プリセット（無関係） / git 6abad8f コミットメッセージ:「要望7: 台数(島数×段数 既定1×1)+製品ライン選択→概算プレビュー(v18.11営業プレビュー)で対応。専用の超簡易フォームは要設計レビュー」— diff実体は要望5のスマホCSS/isMobileのみ（+25/-5行、商品選択への機能追加なし） / 4307-4310: p.salesView分岐はボタン表示制限のみで入力フォームは共通
**不足/差分**: ①シンプル⇔詳細の入力モード切替UIが未実装（商品選択カードは詳細フォーム1本のみ）。②台数の直接入力欄がなく島数×段数からの導出のみ（営業は「段数=1のまま島数=台数」と読み替える運用が必要）。③台数＋ベース有無だけで明細追加できる簡略ハンドラが存在せず、addLineは詳細state前提の単一経路。④v18.14で「既存ビルダーで対応・専用超簡易フォームは要設計レビュー」と内部保留されたが、顧客向け修正まとめ3ファイル（20260605/20260615/20260616）にこの処理方針の記載がなく、顧客合意が取れているか不明。対応するには（a）専用シンプルフォーム＋モードトグルの設計・実装、または（b）「既存フォーム＋既定値で代替」の方針を顧客（吉野氏）に明示して合意を取るかのいずれかが必要。

### 項目8 [missing] 送料を暫定値として表示し、見積作成時に実送料を手入力・登録できる
**根拠**: 4202: h('label',…,'値引き'),h('input',{className:'inp',type:'number',value:discount[0],…}) — 見積オプションの金額入力は値引きのみで送料欄なし（4188-4251行の見積オプションcard全体を実読し確認）／3901: shipping:officialShipping(lines[0],wh,cust,shipInfo?shipInfo.shipping:0) — 見積作成時は常に自動計算値を保存（3843/3872/3947行の仮押さえ系も同様）／1516-1521: function officialShipping(lines,wh,cust,fallback){ if(FREIGHT_OVR&&FREIGHT_OVR.officialMode…)return fr.quoted; return fallback;} — 自動計算の切替のみ／4757-4763: prompt('件名を編集')→prompt('備考を編集')→prompt('値引き金額')→ od.subject=…;od.notes=…;od.discount=…; — 案件編集に送料なし／grep「\.shipping=」該当0件 — 作成後に order.shipping を書き換えるコードが存在しない／6529-6538: 倉庫変更 prompt 後 od.wh=newWh のみで od.shipping 未更新／4932: h('option',{value:'manual'},'手入力') は出荷先住所選択（4916-4936行）、4950: 'その他（手入力）' は荷送人名で、いずれも送料と無関係／4277: title:'…本番送料には自動反映しません'…'運賃表試算: ¥'+fm(fr.quoted)+(fr.approxDistance?'(暫定)':'') — (暫定)は距離近似の注記／4796: h('b',null,'送料: '),detail[0].shipping?'¥'+fm(detail[0].shipping):'-' — 表示専用／4806: '※請求には自動反映しません。実物の送料と照合用' — 送料試算は参考表示／5274: FREIGHT_OVR.officialMode チェックボックス（管理者・全体トグル）
**不足/差分**: ①見積作成フォーム（見積オプション card、4188-4251行付近）に送料の手入力欄（自動計算値を初期値に上書き可能な input）が無い。②作成後の経路（詳細モーダルの編集 4754行、仮押さえタブの数量編集 6484行/倉庫変更 6525行）のいずれにも od.shipping を上書き・登録する手段が無く、値引きでの擬似調整しかできない。③正式送料の表示（4273/4796/6614行）に「暫定」である旨の明示が無い（(暫定)表記は運賃表試算の距離近似注記であり要望の趣旨と別物）。実装するなら order.shipping への手動値保存（例: order.shippingManual フラグ＋入力UI）と、帳票生成（2523行 var ship=order.shipping||0、4411行 var ship=o.shipping||0）への反映、更新見積（3901行 isUpdate 時）で手動値が自動計算に上書きされない保護が必要。

### 項目9 [partial] 倉庫提案の優先順位は送料の安さ。赤倉庫でも他倉庫から部材を送れば充足可能（緑になる）と分かる表示
**根拠**: 3703: /* Sort: OK first, then v01 priority (deplete old stock), then by shipping cost */ ／ 3706: if(a.check.ok&&!b.check.ok)return -1;（第一キー=充足OK） ／ 3708-3711: hasV01&&a.check.ok&&b.check.ok で jp01在庫倉庫を優先（第二キー） ／ 3713: return a.ship.shipping-b.ship.shipping;（送料は第三キー） ／ 4278-4280: !wr.check.ok?h('div',...,'不足: '+wr.check.items.filter(...).map(...).join(', ')):null（赤カードは不足名表示のみ、取り寄せ可否の表示なし） ／ 3721-3722: var okResults=results.filter(...); if(okResults.length===0){（OK倉庫が1つでもあると2倉庫組合せは計算されない） ／ 3799-3801: }else{multiWH[1]([]);}（それ以外は空配列） ／ 4315: '単一倉庫で在庫不足のため2倉庫組合せを表示'（分割出荷提案であり取り寄せ提案ではない） ／ 6346: '🔄 倉庫間移動（取り寄せ）'（在庫編集モーダル内の手動操作。transferStock呼出は6359のみで見積フロー非連携） ／ 3681-3690: lineWhAvail=明細×倉庫の✓×読み取り専用バッジ（4165/4181で描画、赤カードとは無関係）
**不足/差分**: ①送料最優先ソートになっていない：第一キーが充足OK/NG、第二キーがjp01旧Ver在庫優先で、送料は第三キー。要望の「優先順位は送料の安さ」を厳密に満たすには、送料最安の赤倉庫を取り寄せ前提で上位提示する設計が必要。②赤(NG)倉庫カードに「不足部材を他倉庫から取り寄せれば充足可（緑相当）」の判定ロジック・表示が皆無。不足部材ごとに供給元候補倉庫の空き在庫を照合し、取り寄せ後の充足可否を表示する実装が必要。③OK倉庫が1つでも存在すると2倉庫組合せ(multiWH)自体が空になるため、「送料最安の赤倉庫＋取り寄せ」という選択肢がそもそも提示されない。④transferStock(倉庫間移動)は在庫管理画面の手動操作のみで、倉庫提案UIから取り寄せを実行する動線が未連携。

### 項目10 [partial] 自社倉庫間の部材移動（取り寄せ）ができる
**根拠**: 1748-1761: function transferStock(...){ var free=gs(wl,fromWh,key,sub)-gh(...); if(free&lt;qty)return{ok:false,reason:'insufficient'...}; var rOut=isCommonStockKey(key)?_rbDrainCommon(fromWh,key,sub,qty):_rbAddStock(wl,fromWh,key,sub,null,-qty); var rIn=_rbAddStock(wl,toWh,key,sub,null,qty); EDIT_LOG.push(出庫/入庫両脚) } ／ 6346: '🔄 倉庫間移動（取り寄せ）'、6355: if(p.isReadOnly){p.toast(...);return;}、6359: transferStock(em.lineId,...)、6364-6365: broadcastEdit+saveAll ／ 5625・5847-5850: 在庫タブ非扉行の「編集」→startEdit→editModal(isDoor:false) でUI到達可 ／ VM実行(抽出実コード): 通常移動50→30/5→25、空き不足avail=20で拒否、共通在庫40個移動で総量47保存、全6テストPASS ／ 【反証1】5066-5069: vals[w]=(STK.jp2[w][key])||0（jp2単独読み）⇔ 6362: e2.vals[f.from]=gs(em.lineId,f.from,em.key,em.sub)（全ライン合算）⇔ 5108: STK[e.lineId][w][e.key]=v（jp2単独書き戻し）→ VM再現: jp:30/jp2:10/tw:7=総量47で5個移動→保存で総量79（+32増殖）。1052-1054: 「共通在庫(鍵類)は物理在庫が複数ライン(jp/jp2/tw等)のバケットに分散しうる」（本番実態の明言）／【反証2】6313-6335: 扉編集モーダル分岐に移動UIなし、6368: '※…扉は対象外。'、grepで他実装なし ／【小】6349: 移動元selectはWH_IDS.filter(hasStock('jp2',w))のためjp2バケット無し倉庫は移動元に選べない
**不足/差分**: (1) 共通在庫（鍵類）の移動→同モーダル「保存」で在庫増殖する経路の修正が必要。修正案: 6362行のvals更新をgs()合算値ではなくjp2バケット実値(STK.jp2[wh][key])にする、またはsaveEditの共通キー保存を「合算値との差分を_rbDrainCommon/_rbAddStockで適用」する方式に変更。(2) 扉（door/doorH: 色×サイズ3階層）の倉庫間移動が未対応。扉編集モーダルへの移動UI追加、またはtransferStockのcolor対応拡張が必要（顧客が扉の取り寄せを求める場合）。(3) 共通キーで移動元候補がhasStock('jp2',w)に依存し、jp/twバケットのみに在庫がある倉庫を移動元に選べない。gs()>0判定への変更が望ましい。

### 項目11 [partial] 倉庫間移動は入庫予定になり、入庫チェック（消込）で在庫反映され緑になる
**根拠**: 1748-1761: function transferStock(...){... var rOut=isCommonStockKey(key)?_rbDrainCommon(fromWh,key,sub,qty):_rbAddStock(wl,fromWh,key,sub,null,-qty); var rIn=_rbAddStock(wl,toWh,key,sub,null,qty); ...}（移動先へ即時加算、INCOMING登録なし）／ 6359: var r=transferStock(em.lineId,em.key,em.sub,f.from,f.to,q,em.label,editorName[0]);（唯一の呼び出し＝在庫編集モーダルの「移動」ボタン。入庫予定化オプションなし）／ 7112: INCOMING.push({id:nextIncId++,...,status:'予定'});（INCOMING生成は手動追加モーダルのみ。他は219-222行の同期マージだけ）／ 7078-7095: 「入庫処理」onClickで STK[inLine][inc.wh]...+=it.qty; inc.status='入庫済';（消込の仕組み自体は存在）／ 7077: className:'bg '+(inc.status==='入庫済'?'bg-g':'bg-o')（入庫済=緑バッジ）／ 764-784: orderReadiness(o){... free=avail(it.line,it.wh,it.key,...); if((free||0)<n.qty)shortCount++;}（空き在庫を直接参照＝移動即時反映で入庫チェックを経ずに緑化）／ 4682: '🟢 入庫済・出荷可'／ 修正まとめ_20260615.html 245: 「倉庫間の取り寄せ（移動）…本社から不足倉庫へ部材を移動。在庫が緑になれば次工程へ。」（即時反映実装として顧客報告済み）
**不足/差分**: (1) 倉庫間移動実行時に移動先在庫へ即時加算せず、INCOMING へ status:'予定' のレコードを自動登録する処理が未実装（transferStock 1757行の rIn 即時加算を入庫予定化に置き換える必要）。(2) 入庫処理（7078-7095行）で初めて移動先在庫に加算される動線への接続が未実装。(3) 移動元の減算タイミング（移動指示時に引くか、入庫確定時に引くか、輸送中在庫をどう扱うか）の仕様が未確定で顧客確認が必要。(4) 現状は orderReadiness が空き在庫直参照のため、移動すると消込前に🟢になってしまい「入庫チェックで緑」のゲートが効かない。現行の代替は手動運用（在庫編集で移動元を減らし、入庫予定を手入力→入庫処理）のみ。

### 項目12 [partial] 入庫予定のステータスを変更できる
**根拠**: 2268: var INCOMING=[];/* {id,...,status:'予定'|'入庫済'} */（ステータスは2値のみ）／ 5248: h('button',{...onClick:function(){showIncoming[1](true);}},'入庫予定')（読み取り専用でも無条件表示）／ 7069: h('th',null,'状態'),h('th',null,'操作')（一覧に状態・操作列）／ 7078: inc.status!=='入庫済'?h('button',{...onClick:function(){...（唯一の遷移ボタン、isReadOnlyガード無し）／ 7095: inc.status='入庫済';bump[1](...);p.toast('入庫処理完了: INC-'+inc.id);saveAll();（一方向遷移の実体）／ 7112: INCOMING.push({...,status:'予定'});（追加は常に予定で作成）／ 219-222: if(opts.replace){INCOMING=doc.incoming;}else{...INCOMING.push(i)...INCOMING[ix]=i;}（同期マージのみ、削除伝播無し）／ grep結果: INCOMING.splice・INCOMING.filter・削除ボタンは0件／ 対照: 5078: if(p.isReadOnly){p.toast('読み取り専用モード中は在庫編集できません');return;}、4588: if(p.isReadOnly){p.toast('読み取り専用モード中はステータス変更できません');return;}（他機能はガード有り）
**不足/差分**: ①入庫予定のキャンセル・削除機能が無い（誤登録したINC行を消す手段が一切無い。削除墓標機構は案件ORDERSのみでINCOMING未対応）。②作成後の予定日・数量・倉庫・備考の編集UIが無い（追加モーダルは新規作成専用）。③入庫済→予定への差戻しが無く、誤って入庫処理すると在庫が恒久加算され、復旧は手動の在庫編集のみ。④入庫処理（7078行）・入庫予定追加（7107行）にp.isReadOnly/営業ロールのガードが無く、読み取り専用ユーザーでも共有在庫STKを加算・予定追加できる（他の在庫変更系は全てガード済みで、アプリの権限モデルと不整合）。

### 項目13 [missing] NGの工場（出荷を止めたい倉庫・工場）を抑える管理機能
**根拠**: 該当機能なし。518-526: `var WH_MAP={honsha:{n:'本社倉庫',s:'本社',pref:'東京都',region:'kanto'},...}` に有効/無効・NGフラグ類は無く、517: `var WH_IDS=['honsha','dai1','dai2','kashiwa','sapporo','amago','kansai']` は常に全倉庫が候補。倉庫比較UIも 4262: `whResults[0].filter(function(wr){return !wr.noLine;})`（取扱ライン無しの除外のみ）で全倉庫を提案し、4272 の `wr.check.ok?'OK':'NG'` は在庫充足判定の表示であって出荷停止管理ではない。'NG工場|出荷停止|ブロック|除外' 等のgrepで倉庫・工場を提案から外す設定/UI/ハンドラは一切ヒットせず。
**不足/差分**: 倉庫・工場単位の出荷停止フラグ、管理画面での設定UI、見積/出荷提案からの除外ロジックのいずれも未実装。実装するなら WH_MAP へ disabled フラグ＋管理者設定UI＋whResults/出荷指示のフィルタが必要。

### 項目14 [partial] 大口時に混載便とチャーター便の金額を比較しコスパ判断できる表示
**根拠**: 実装済み部分: 4738: title:'案件詳細 — '+detail[0].no のモーダル内、4832: '混載 vs チャーター（参考比較）'、4834: '混載 運賃原価: ¥'+fm(mix)+(cheaper==='mix'?' ◀安い':'')、4835: ['2t','4t','10t'].map(...sk+'=¥'+fm(ch.prices[sk].price)...)、4836: '推奨車格 '+ch.suggest+'=¥'+fm(ch.suggestPrice)+(cheaper==='charter'?' ◀安い':'')。計算基盤: 1524-1532: var CHARTER_TABLES={kansai:{...'2t':[20000,22000,...]},sapporo:{...}}（尼高運輸PDF実データ）、1548-1555: charterCost() が全車格価格＋積載量基準の推奨車格を返す。欠落の根拠: 4823: if(!fr.ok)return null;（1511: reason:'over_weight_charter' に distance:dd.km が含まれ計算可能なのに表示されない。4818: '運賃表の上限超過 → チャーター便対象' の文言のみ）、1535-1539: charterTableForWarehouse が amago/kansai/sapporo 以外 null（517: WH_IDS=['honsha','dai1','dai2','kashiwa','sapporo','amago','kansai'] の4/7倉庫が対象外、4827: 注記のみ表示）、618: var CHARTER_KG=1000;（重量基準のまま、grep '100個|大口|コスパ'=0件）、4275: wr.ship.charter?'チャーター便 +¥30,000'（見積画面は簡易表示のみ）。別実装なしの根拠: charterCost 呼び出しは 4825 の1箇所のみ（grep全件確認）、運賃編集モーダル(5265-)にチャーター比較なし、修正まとめ3ファイルに「チャーター」記載0件。
**不足/差分**: (1) over_weight_charter（真の大口＝運賃表上限超過）時にチャーター金額が表示されない: 4823の !fr.ok ガードを reason==='over_weight_charter' の場合は通過させ、fr.distance を使って charterCost を呼び「混載: 運賃表対象外／チャーター: 2t=…4t=…10t=…」を表示する差分が必要。(2) 本社系4倉庫（honsha/dai1/dai2/kashiwa）発のチャーター料金表が未提供で比較不能（顧客からの料金表入手待ち）。(3) 「100個以上」等の数量基準トリガー未実装（CHARTER_KG=1000 の重量基準のまま。吉野さん回答「チャーター判定は数量」と不整合）。(4) 見積作成時の倉庫比較カードには比較表示がなく「チャーター便 +¥30,000」の定額表示のみ（判断時点が受注後にずれる）。(5) 追加発見: 札幌発25km以下は2t/4tとも料金nullで ch.ok=false となり、4827が table存在時はnullを返すため注記もなく無言で非表示になる。

### 項目15 [partial] チャーター便等の運賃を管理側（吉野=管理者）のみ変更できる機能
**根拠**: /Users/takaishouhei/Claude案件/株式会社LEC/在庫管理システム/index.html: 【実装済み部分】5261: roleOf(p.userName)==='admin'?h('button',{...onClick:function(){showFreightEdit[1](true);}...},'🚚 運賃編集'):null（管理者のみ表示）／5263: var isAdm=roleOf(p.userName)==='admin'; 5268: !isAdm?h('div',...,'※管理者のみ編集できます'):（二重ガード）／2288: function roleOf(nm){var u=findUserByName(nm);return u?(u.role||'user'):(nm?'user':'');}（実在の権限分岐）／5297: if(p.isReadOnly){p.toast('読み取り専用モード中は編集できません');return;}／1409: var ov=effRateCell(tableKey,wi,di);if(ov!=null)rate=ov; 1444/1448: FREIGHT_OVR.shokei/prefKm が計算に反映／1456-1460: recordFreightHistory で履歴記録、5319-5322: 履歴テーブル表示。【不足の実証】618-619: var CHARTER_KG=1000; var CHARTER_FEE=30000;（ハードコード）／664: if(tw>CHARTER_KG)ship+=CHARTER_FEE;（本番簡易送料に直結、編集不可）／4275: 'チャーター便 +¥30,000'（表示文字列もハードコード）／1524-1533: CHARTER_TABLES/CHARTER_CAP ハードコード、1541-1555: charterRate/charterCost にオーバーライド層なし（4822-4838行は読み取り専用の参考比較表示のみ）／1451-1454: effRateCell は FREIGHT_OVR.rates を読むが、grep で書き込み箇所ゼロ（1438行コメントと1452行読取のみ＝死蔵レイヤー）。編集モーダルの入力欄は 5282-5283（諸経費2件）と 5290-5292（都道府県距離）のみ、保存ハンドラ 5299-5308 も shokei/prefKm しか書かない／1455: saveFreightOverride は localStorage('lec_freight_override') のみ。158-172: buildCloudDoc() の同期対象は orders/nextON/held/stk/incoming/nextIncId/custs/users/editLog/customParts/customIrisu/deletedNos/stkSnapshot で freight override 不在／5270: 「全端末で共有されます（localStorage）」はlocalStorageが端末ローカルである以上、事実と不一致。【別実装なしの確認】7169: showCostMaster は「仕入れ単価マスタ」で運賃と無関係。修正まとめ_20260615.html 187-198/244行は運賃表500kgズレ修正と正式送料オプトインのみで、チャーター運賃編集の実装報告なし。修正点まとめ_20260605.html にチャーター項目なし。
**不足/差分**: (1) チャーター便運賃の編集機能が皆無: CHARTER_FEE/CHARTER_KG/CHARTER_TABLES にオーバーライド層（FREIGHT_OVR.charter等）と編集UIを追加する必要がある。現状は金額改訂のたびにコード修正+再デプロイが必要。4275行の '+¥30,000' 表示文字列も定数参照に変える必要あり。(2) 混載/西濃の地別運賃マトリクス本体のセル編集UIが無い: effRateCell が読む FREIGHT_OVR.rates への書き込みUI（運賃表グリッド編集）が未実装。編集可能なのは諸経費2件と本社発都道府県距離のみ。(3) 変更の端末間共有が無い: lec_freight_override を buildCloudDoc/applyCloudDoc の同期対象に含める必要がある。現状は吉野さんが変更しても自端末のみ反映で、5270行のUI文言（全端末で共有）が実態と乖離しており誤解を招く。

### 項目16 [partial] 商品（明細）ごとにどの倉庫の在庫を使用するかを選択できる（例: B倉庫から100個）
**根拠**: 3680-3691: /* v18.20 要望16: 各明細が供給可能な倉庫を可視化（…読み取り専用） */ function lineWhAvail(l){… '供給可:' + WH_IDS.map(… checkAllLinesWH([l],w).ok … '✓'/'×')} — クリック不可のspanバッジのみ(onClickなし)。描画は4165行・4181行の明細カード内。 4264: onClick:function(){selWH[1](wr.whId);selMulti[1](null);} — 倉庫選択は案件全体で単一。 4319: onClick:function(){selMulti[1](idx);selWH[1](null);} — 2倉庫は事前計算済みペアの選択のみ。 1878: var takeA=Math.min(freeA,dc.qty),takeB=dc.qty-takeA;(holdBOMMulti) — 2倉庫時の明細×倉庫割当は主倉庫A優先の自動計算でユーザー指定不可。 3865: ln._actualHeld=actualM;ln._heldWh=whA; — 全明細に一律で主倉庫をセット。 6529-6538: prompt('変更先の倉庫IDを入力…')→od.wh=newWh;od.wh2=null; — 倉庫変更も案件全体単位。 6352/7119: WH_IDS.mapによるselectは倉庫間移動先・入庫先倉庫のみで明細用ではない。 修正まとめ_20260615.html 246行:「供給可能倉庫の可視化…明細ごとに『どの倉庫から供給できるか』を表示」— 顧客報告も可視化と明記。 memory/project_lec_phase2_progress.md 24行:「P4残…要望16(部材ごと倉庫の手動選択UI=現状は複数倉庫ペアで自動割当)」。
**不足/差分**: 明細行に倉庫指定セレクト(＋「B倉庫から100個」のような数量分割入力)が無い。仮押さえ処理(doHoldAndQuote/doReserveOnly/doHoldAndQuoteMulti)は selWH/selMulti の案件単位倉庫しか受け取らず、明細ごとの指定倉庫を _heldWh/_actualHeld._wh へ反映する経路が存在しない。2倉庫組合せ時の割当も holdBOMMulti の自動計算(主倉庫優先)固定で上書き手段が無い。必要な差分: 明細カードに倉庫セレクト(任意で数量分割)を追加し、hold系関数が明細指定倉庫を優先して確保、_heldWh/_actualHeld._wh・出荷指示書/BOM Excelの倉庫分配に反映する実装。

### 項目17 [implemented] エクセル関連: 在庫削除（削除15項目）とレールセット（レール新方式: 在庫4種・見積6種）の反映
**根拠**: v16.0コミット f25751f「削除15項目・レール新方式」で実装、現作業ツリーで残存確認済み。(1)パーツ単品追加 PART_ADD_DEFS(380-445行)に削除15項目（トッテ旧ハンドル/ダイヤル錠 井門/旧型/検索キー/マスターキー/シリンダー錠 無印・マスター組/旧レールセット/レール短/長/ネームプレート/ネーム厚紙/シリンダーF・B・Cマスター）が存在しないことをgrepで確認。レール見積6種は 431行:`{k:'railSideSML',l:'SMLXL共通サイドレール'}`〜436行:`{k:'railSetSS',l:'SS・3S共通レールセット',expand:[{key:'railSideSS',qty:2...},{key:'railBackSS',qty:1...}]}`。(2)セット分解ハンドラ 3653行:`if(pd.expand){/* v16: レールセット等を構成部材へ分解して複数明細追加 */`（addPartLine内で側×2+背×1の明細に展開、実UI動線）。(3)在庫一覧・倉庫別Excelの行定義 structRowsAll 5175-5178行:`{label:'SMLXL共通サイドレール',key:'railSideSML',cat:'option'}`等レール4種あり・削除項目なし（5132-5202行を通読）。(4)在庫表Excel snapRows 5479-5482行:`snapRows.push({item:'SMLXL共通サイドレール',key:'railSideSML'})`等4種あり・削除項目なし（5427-5500行を通読）。(5)在庫一覧タブ uniRows(5641行〜)も削除項目なし・5723-5727行にレール4種。(6)127行:`var STK_LATE_ADDITIONS={};/* v16: レール新方式(在庫4/見積6)へ移行。旧railShort/railLong補充は廃止 */`
**不足/差分**: 要望範囲（Excel・在庫表・見積）は完了。周辺画面に不整合が残る: 仮押さえ一覧/出荷済み一覧サブタブの行定義uniR（5908-5923行・6188-6203行:`uniR.push({item:'トッテ（旧ハンドル）',...key:'handle'...})`〜`key:'cylinderCMaster'`）には削除項目の一部（旧ハンドル/シリンダー錠/マスター組/ネームプレート/ネーム厚紙/F・B・Cマスター）が表示され続け、逆に新レール4種は未掲載のためこの画面ではレールの押さえ数を編集できない。また未使用のPART_DEFS(369-374行)に旧キーがデッドコードとして残存。

### 項目18 [partial] 鍵（錠前）はDBと同じ数・同じ名前で在庫と紐付き、パーツ単品追加の品揃えと統一されている
**根拠**: (1)332-336行コメント:`v18.21 要望18: 商品選択の錠前を実在庫キー(部材在庫一覧/パーツ単品 cat:'lock')と同名・同数で紐付け`＋`v18.25 要望⑦: 商品選択の錠前を「パーツ単価追加(cat:'lock')」と同じ品揃えに統一`。(2)LOCK_TYPES本体 337-358行: 338行:`{v:'dialH110new',l:'ダイヤル錠 H110(新)'...}`等、旧H110/H111・H112・DST・南京錠・トッテ・シリンダーカム長/短各種・内筒交換・コインリターンを実在庫キーで定義（マスター組は除去済み）。(3)UI露出 4070行:`LOCK_TYPES.filter(...).map(function(lt){return h('option',{key:lt.v,value:lt.v},lt.l);})`（商品選択の錠前セレクト、パーソナルはnone除外）。(4)在庫紐付けの実効性: 893行:`bom.push({key:lock,qty:lockN,label:(partLabelJP(lock)||...)`でBOMに実キーで積み、914行:`stock=gs(line,wh,b.key)`＋717行COMMON_STOCK_KEYS＋1798-1802行:`function normalizeStockLine(line,key){if(isCommonStockKey(key))return 'jp2';}`で鍵類は全ライン共通在庫として集約・引当される。(5)顧客報告 修正まとめ_20260616.html 133-138行「主要な錠前を商品選択でも選べるように追加」「マスター組・検索キー・カラー南京錠など在庫管理専用の一部品目は従来どおりパーツ単品追加から（意図的に外す）」。
**不足/差分**: ①346行:`{v:'cylinder',l:'シリンダー錠',s:'シリンダー錠'}`＝汎用シリンダー錠が暫定残存。STKに'cylinder'の在庫値は存在せず（在庫はcylinderShort/Long系のみ。576行はIRISU定義であり在庫ではない）、選択すると常に欠品表示になる。334行コメント:`汎用cylinderは在庫に対応キーが無く...要吉野確認のため暫定保持`＝顧客確認待ちで未解決。②パーツ単品(cat:'lock')との完全一致ではない: dialOpnus/シリンダーD・Lマスター/3桁カラー南京錠/ダイヤル錠検索キー4種はLOCK_TYPES未掲載（ただし修正まとめ_20260616.htmlで「在庫管理専用品目は意図的除外」と顧客合意済みの体裁）。

### 項目19 [implemented] 案件一覧で仮押さえや出荷指示のステータスが見える
**根拠**: 3262行: TABS=[...{k:'orders',l:'案件管理'}...]（全ロール共通でタブ表示）/ 4584行: var fl=ORDERS.filter(function(o){return filter[0]==='all'||o.status===filter[0];})（初期値'all'=全件表示、4581行 filter=us('all')）/ 4669行: h('th',null,'ステータス')（一覧にステータス列）/ 4680行: h('span',{className:'bg '+(STATUS_CLS[o.status]||'bg-b')},o.status)（バッジ表示）/ 21-22行: .bg{...} .bg-o{background:var(--op)...} .bg-tl{background:#E0F2FE...}（仮押さえ=オレンジ、出荷指示=水色のCSS実体）/ 3843-3844行: var order={...status:'仮押さえ'...}; ORDERS.push(order)（仮押さえは必ずORDERSに入る。3872-3873行・3947-3948行のreserveOnly仮押さえも同様）/ 4642行: order.status='出荷指示'（confirmShipOrderで実遷移）/ 4664行: STATUS_FLOW.map(...)（仮押さえ・出荷指示を含むフィルタボタン）/ 764-785行: function orderReadiness(o){...}（🟢入庫済・🟠発注待ち補助バッジの実体、読み取り専用）/ 4845-4856行: ステータス遷移ステッパー（changeStatus/rollbackStatus）
**不足/差分**: なし。強いて言えば「見積作成」ステータスは案件が仮押さえで直接作成されるため差し戻し時以外ほぼ空だが、項目19の要望（仮押さえ・出荷指示の可視化）には影響しない。

### 項目20 [partial] 上部のボタン等の文字をデスクトップ版で大きく
**根拠**: 12: 「.tab{padding:7px 18px;...font-size:14px;font-weight:700;...}」— git履歴（コミット f25751f「v16.0 P1 第2フェーズ着手（…タブ文字拡大）」）で font-size:10px→14px・padding:5px 14px→7px 18px に拡大されたことを確認。デスクトップのベースCSSなのでメディアクエリなしで常時適用。9: body font-size:14px（v11.0で13px→14px）。上部4タブ（顧客選択/在庫確認・見積/案件管理/部材在庫一覧）は3287-3289で .tab クラスにより描画。
**不足/差分**: 拡大されたのはタブ4個のみ。同じヘッダ上部の他要素は据え置き: 3290 ログイン者名 fontSize:8／3292・3293-3297 「👥 ユーザー管理」「切替」ボタン fontSize:7（極小）／13: .ver{font-size:8px}／11: .logo span{font-size:10px}。ページ内上部の操作ボタン（16: .btn{font-size:10px}、17: .btn-s 9px/.btn-xs 9px）も小さいまま。要望が「タブの文字」のみを指すなら充足だが、「ボタン等」を上部の各種ボタン全般と解するなら未対応要素が残る。

### 項目21 [implemented] 商品選択の「在庫確認・見積」を押すと日本製Ver.2がデフォルト、島数・段数のデフォルトは1,1
**根拠**: 3562: 「var ln=us('jp2'),sz=us('M'),tp=us('standard'),cl=us('pb'),lk=us('dialH110new');」— 製品ラインの初期値が jp2（296: 「{id:'jp2',name:'日本製 Ver.2',mark:'★'}」）／3566: 「var isl=us(1),tier=us(1);」— 島数・段数の初期値が1,1／UI反映も確認: 3982-3984: 製品ラインボタンが「className:'btn btn-xs '+(ln[0]===l.id?'btn-nv':'btn-ol')」で描画され初期表示で日本製Ver.2が選択状態、4061-4062: 「島数（列）」「段数」入力が value:isl[0]/value:tier[0] で初期値1を表示。localStorage等から初期値を上書きするコードは無し（repeatOrder=リピート発注時のみ意図的に案件値を復元）。
**不足/差分**: なし

### 項目22 [implemented] 商品在庫Excelアップロード→差分表示→反映（顧客自身で在庫更新）。集計在庫表レイアウト対応
**根拠**: 差分→反映の2段階UIと集計レイアウト対応の両方を実コードで確認（PartsPage内）。(1)取込UI 5334行:`'📥 在庫表取込（差分→反映）'`＋5335-5359行のfile input onChange: 5343-5344行:`var parsed=parseStockImport(sheets); if(!parsed.length)parsed=parseAggregateStock(sheets);`（標準形式→0行なら集計レイアウトへフォールバック=v18.26要望⑨、5342行コメント）。(2)集計レイアウトパーサ parseAggregateStock 1619-1700行: 1623行コメント`『現在庫数』は要求しない`、1638-1655行で版アンカー(Ver.1/Ver.2/台湾/Sα)を列位置から検出し日本製Ver.1/Ver.2横並びに対応、1656-1664行で倉庫列特定（小計/合計除外）、1677行:負値スキップ、1682-1698行:共通在庫=最大値/非共通=合算の冪等集約。(3)自動マッピング mapStockItem 1188-1233行: 扉は品名色×色列サイズの3階層(1202-1207行)、ボード/共通部材/ネジ/錠前(1224-1232行、鍵は共通プール)。(4)差分表示 buildStockDiff 1702-1720行＋差分モーダル5361-5396行: 5384行にシステム/取込/差の列、5375行に解析行数/マップ/差分/未対応件数、5377-5382行に0行時の形式案内（「いつもの集計在庫表（日本製Ver.1／Ver.2の2表・台湾製）に対応」）。(5)反映 5364-5371行:`h('button',{...disabled:p.isReadOnly,onClick:function(){...var n=applyStockImport(di.diffs,editorName[0]);broadcastEdit(...);saveAll();`（confirm付き・読取専用ガード・全端末同期）。applyStockImport 1724-1745行はスナップショット非bump・EDIT_LOG記録・鍵類jp2集約・扉3階層対応(1727-1734行)で冪等。
**不足/差分**: 軽微な制約のみ: ①レール/特殊部材/黒側板/色・サイズ判別不能行は未対応行として反映対象外（1233行:`return null;/* それ以外(レール/特殊部材/黒側板等)は未対応 */`、UIでも5395行に明示）。レール4種は在庫項目化済みなのに取込では更新できない。②ダイヤル錠H110/H111は新ラッチキー(dialH110new/dialH111new)へマップ(1232行)する仕様割り切り。③本監査対象のv18.26は未コミット（本番=v18.25/13bcd00には未反映）で、顧客実ファイルでの動作実証はコードからは確認不能。

### 項目23 [partial] 出荷指示書の既存フォーマット完全一致（画面・Excel、台湾製等の型別様式）
**根拠**: 実装確認済みの根拠: (1) index.html:1238「var SHIP_TEMPLATES={"出荷指示書原本（SML）日本製":{...}」に5型(SML/SS・3S/XL/台湾製/Sα)の全セル・結合を静的複製。(2) v18.22数量列修正は1339行「var qTot=((typeof tpl.iriCol==='number')?tpl.iriCol:11)+1,qBox=qTot+1,qBara=qTot+2;」で実装され、SML/SS/Sα(iriCol=11)→M/N/O列、XL/台湾製(iriCol=1)→C/D/E列となりテンプレ実物の列位置と一致することを確認。(3) v18.25納品先名は1326-1329行「setCell('Q5',hv.name)/setCell('Q10',hv.phone)/setCell('R12',hv.postal)/setCell('Q13',hv.address)」・1332-1333行「setCell('G4',hv.name)/setCell('G12',...)」で、テンプレ実物の結合構造(Q5:Y6/Q10:Y10/R12:Y12/Q13:Y14/G4:I5/G12:I13)と突合し正しい入力セルであることを確認。1319行でendUser→shipTo→顧客名の優先で納品先名=顧客名を実現。(4) 錠前行追記は1365行「cells[...]='＜錠前（鍵）＞'」以下で様式末尾に品名+数量を追記。(5) 3シート統合は2890-2891行「var shipTpl=buildShipInstruction(order); if(shipTpl&&shipTpl.ws){XLSX.utils.book_append_sheet(wb,shipTpl.ws,...)}」。(6) UI露出は4702-4711行「📋 出荷指示書(様式)」ボタン(buildShipInstruction→XLSX.writeFile)と4700行「📊 出荷Excel」。一方、テンプレ実物(ロッカー資料/出荷指示書フォーマット.xlsx、暗号化なし、xl/workbook.xml)は4シート構成「出荷指示書原本（SML）日本製_改良版／出荷指示書原本(SS・３S)日本製_改良版／出荷指示書原本 (台湾製Sα）／出荷依頼原本(台湾製)」でXLシートが存在せず、コード内蔵テンプレとセル単位の相違が多数残る(下記gaps)。
**不足/差分**: テンプレ実物(出荷指示書フォーマット.xlsx)とのセル単位の残差分:【SML様式】A40/L40: テンプレ「レールセット」入数400 vs コード「サイドジョイント」入数300(部材行の置換が未反映)／Q24: テンプレ「※請求書、納品書、物品受領書、鍵資料は同梱になります。」 vs コード「※お客様直送の為、綺麗な箱で…※発送後に送り状を…」(注意書き文言が旧版)／Y8: テンプレ「様」(荷受人欄敬称) vs コード無し／V3: テンプレ日付シリアル46023 vs コード無し／U35〜Y35: テンプレ「北/東/西/本/KE」 vs コード「北」のみ、U36〜Y36「/」×5 vs 「/」×1、X37〜X39「）」・Y37〜Y39「/」 vs 無し(社内確認欄の拠点列欠落)／M42・N41: テンプレは数式SUM(M5:M40)/SUM(N5:N40) vs コードは値0固定のまま再計算せず→出力Excelの「全パーツ計」「正箱数計」が常に0(機能バグ)／シート名: 「…_改良版」 vs 出力名「出荷指示書（SML）日本製」／結合: コードのみのAB16:AJ30系20個(テンプレに存在しない旧版由来)。【SS・3S様式】Q24: テンプレ「※請求書、納品書、物品受領書、組み立て資料は同梱になります。」 vs コード旧文言／Y8「様」・V3(46143)・U35〜Y39拠点列・M42/N41合計0・AB系結合20個: SMLと同じ欠落／シート名: テンプレ「(SS・３S)…_改良版」(全角３) vs コード「(SS・3S)」(半角3)。【Sα様式】Q24: テンプレ2行目「※発送後にドライバーさんのご連絡等をメールにてお知らせください。」 vs コード「※発送後に送り状を弊社までメールをお願いします。」／AA5:AB40: テンプレのパーツ名/入数参照表(サイドジョイント300・SSサイドジョイント80・トップジョイント(葉っぱ)2500・ドアポケット108等16行)+AA19/AA20注記(ダイレクト様引取り等) vs コード無し／AD21:AL59: テンプレの荷送人4ブロック((株)LEC/㈱プラロッカー/プラスチックロッカー㈱/AD51注記)+結合19個 vs コード無し(refがA1:AJ62のためAK/AL列自体が範囲外)／Y8「様」・V3(46057)・U35〜Y39・M42/N41合計0: 同様の欠落／A1: コードは倉庫名で上書きするため「新柏倉庫株式会社　様」の「様」が消える。【台湾製様式】シート名: テンプレ「出荷依頼原本(台湾製)」 vs コード「出荷指示書原本(台湾製)」(依頼vs指示書)／A1: テンプレ「東京本社」 vs コード「本社」(実出力は倉庫名で上書き)／G25: テンプレ「※発送後にドライバーさんのご連絡先等をメールにてお知らせください。」 vs コード「※お客様直送の為…※発送後に送り状を…」／L16:T30: コードのみに荷送人4ブロック(グローバル含む)+結合20個が残存(テンプレ実物では削除済み、寸法A1:I38に対しコードrefはA1:T38)。【XL様式】テンプレ実物にXLシートが存在しない(4シートのみ)のに、コードは「出荷指示書原本（XL）」を保持し出力する→どの版が正か顧客確認が必要。【全様式共通】(a)罫線・列幅・行高・フォント・チェックボックス(フォームコントロールctrlProp×29個)・印刷範囲(Print_Area)は一切再現されず、値と結合セルのみの出力(SheetJS CE制約)→見た目は原本と一致しない。(b)納品日時: 1330行setCell('Q17',…)はテンプレ既存値「2026/」があるため書き込まれず、SML/SS/Sαでは納品日が様式に反映されない(XL/台湾のG16は反映される)。(c)画面表示・印刷(openQuote shipping、2458/2477行)は従来の明細一覧形式のままで、様式レイアウトではない(タイトルに【SML用】等を付すのみ)。※コード内蔵5型は2026-06-05受領の別ファイル(パスワード付/5シート版)由来の可能性が高く、ロッカー資料の改良版(2026-02-18作成)とどちらが「既存のフォーマット」の正本かの確認、特にXL様式の要否・SML40行目(レールセットvsサイドジョイント)・注意書き文言3種の確定が必要。

### 項目24 [implemented] ステータスを戻せる（例: 受注確定→仮押さえに戻す）
**根拠**: 527行: var STATUS_FLOW=['見積作成','仮押さえ','受注確定','出荷指示','出荷完了','納品完了'] / 4686行: onClick:function(){detail[1](o);}},'詳細'（モーダル導線） / 4850-4856行: var isBack=idx&lt;curIdx; ... onClick:function(){if(isNext)changeStatus(detail[0],s);else if(isBack)rollbackStatus(detail[0],s);}（◀赤枠ボタン活性） / 4612-4632行: rollbackStatus() confirm→rollbackInventory→order.status=target→broadcastOrderUpdate+saveAll / 1140-1154行: rollbackInventory() 出荷済→未出荷はrestoreShippedStock、toHeldならreHoldFromSnapshot、held→見積はreleaseOrderHeld、受注確定→仮押さえはneutral(在庫不変) / 1075-1101行: restoreShippedStock() EDIT_LOG「出荷消費」逆仕訳＋e._rb=true二重戻し防止＋snapフォールバック / 2321行: saveAll(){saveOrders();saveStock();saveHeld();saveIncoming();saveEditLog();saveCusts();}（永続化）
**不足/差分**: 判定を覆さない理論上の残課題のみ: (a) 1077行の label indexOf 前方一致は注文番号が4桁到達時(QT-2026-100 vs QT-2026-1000)に他案件の出荷消費ログを誤マッチしうる（現行 padStart(3)・年間999件までは発生しない）。(b) v17以前に出荷された旧案件は _shippedActualHeld が無く、差し戻し時の再引当が無音スキップされる(1108行)レガシーデータ限定の穴。

### 項目25 [implemented] 代理店経由の案件はエンドの会社の住所を入力できる
**根拠**: 4227-4249行: h('label',...,'エンド納品先（代理店経由の場合・任意）')＋納品先名/〒/住所/TELのinput4本がendUser stateにonChangeバインド（見積オプションカード内、4188行 lines[0].length>0 で表示）/ 3574行: var endUser=us({name:'',postal:'',address:'',phone:''}) / 3843・3872・3901・3947行: 全作成経路で endUser:(endUser[0]&&(endUser[0].name||endUser[0].address))?Object.assign({},endUser[0]):null をorderへ保存 / 2315行: saveOrders()がJSON.stringify(ORDERS)で丸ごと永続化（163行のSupabase同期docにもorders:ORDERS）/ 2572行: 見積HTML『エンド納品先:』行 / 2744-2746行: 出荷Excelに『エンド納品先』行 / 1317-1319行: 公式様式 var eu=order.endUser...でhv生成→1326-1334行でQ5(納品先名)/Q10(TEL)/R12(〒)/Q13(住所)へ充填 / 4599-4601行: 出荷指示時shipForm[1]({shipToName:eu?eu.name:...})で自動セット / 4924-4933行: 出荷モーダルselectのvalue==='enduser'ハンドラがフォームへ実充填＋option『エンド納品先（この案件）』/ 3624行: リピート発注でendUser復元 / 4469行: 案件詳細表示
**不足/差分**: 実装済みのため必須の不足なし。軽微な留意点: (1)3600-3606行の顧客切替クリアeffectにendUserが含まれず、前顧客のエンド住所が残留しうる (2)1319行で公式様式はendUserをorder.shipToより優先するため、出荷モーダルで宛先を変更した場合に公式様式と食い違う可能性 (3)DEALERSは2250-2252行のサンプル2件ハードコードで登録UI無し（エンド住所自体は手入力で対応可能）。

### 項目26 [implemented] 既存顧客の2回目・3回目の発注（リピート）を管理できる
**根拠**: 4689-4697行: h('button',...title:'この案件の内容をコピーして新規見積（リピート発注）',onClick:function(){ if(p.isReadOnly){...return;} var rc=CUSTS.find(...); p.setCust&&p.setCust(rc); p.requestRepeat(o); p.setTab&&p.setTab('inv'); }},'🔁 コピー再発注') / 3302-3303行: InvPageへ repeatOrder:repeatOrder[0],clearRepeat / OrdersPageへ requestRepeat:function(o){repeatOrder[1](o);} / 3610-3628行: ue(function(){var ro=p.repeatOrder;if(!ro)return; var cloned=cloneLinesForRepeat(ro.lines); lines[1](cloned); if(ro.wh)selWH[1](ro.wh); quoteOrder[1](null);showQuote[1](false); quoteSubject[1](ro.subject||''); ... endUser[1](...); if(typeof doCheck==='function')doCheck(cloned); p.toast('リピート発注: '+(ro.no||'')+' の内容を読み込みました...'); p.clearRepeat&&p.clearRepeat();},[p.repeatOrder]) / 1156-1161行: cloneLinesForRepeat — delete n._actualHeld;delete n._heldWh;delete n._hadActualHeld;delete n._shippedActualHeld;delete n._partialReleases（前回引当は非継承・元配列非破壊） / 3898-3908行: var existing=quoteOrder[0]; var isUpdate=existing&&existing.status==='見積作成'&&existing.cid===cust.id; var no=isUpdate?existing.no:'QT-2026-'+String(nextON++)... → repeat時はquoteOrder=nullなので必ず新規番号でORDERS.push（2回目・3回目が別案件として蓄積） / 3841-3844行: doHoldAndQuoteも新番号採番＋ORDERS.push / 3442-3446,3497行: CustPage 既存顧客の検索(名前/ID/市区町村/担当)＋『選択 →』(p.selectCust→3301行でinvタブ遷移) / 99行: react@18.2.0（effect定義順=3600行クリア→3610行読込が保証）
**不足/差分**: 判定を覆すほどではないが2点の残課題を発見。(1)【監査者指摘と同旨】案件一覧のフィルタはステータスのみ(4663-4664行)で顧客名検索・顧客単位の発注履歴ビューが無く、件数増加時に特定顧客の過去案件を探しにくい（顧客名列4675行の目視頼み）。(2)【新規発見・潜在バグ】cloneLinesForRepeat(1156-1161行)は返品処理(6248行: ln._returned=true)で付与される _returned フラグを除去しない。返品処理済み案件をコピー再発注すると新案件の明細に _returned が残留し、computeOrderParts(4967行: if(ln._returned)return;)が該当行を部材集計から除外→仮押さえ表(5943行)・出荷グリッド(6223行)・部材集計Excelで新案件の部材が表示されない一方、doHoldAndQuote(3835-3838行)は実在庫を引き当てるため表示と在庫消費が不整合になる。返品済み案件のリピートという限定シナリオのため implemented 判定は維持するが、delete n._returned の1行追加で修正可能。

### 項目27 [partial] 在庫表と顧客情報を毎週定期バックアップし吉野さんに送付
**根拠**: 1762-1763行: コメント「v18.21 要望28: …静的サイト構成のため自動メール送付は不可。ワンクリック書き出し＋週次リマインド(7日)で運用する」 / 1764-1797行: exportFullBackup() — 『顧客一覧』(1768-1770行)＋『在庫データ』(1772-1791行)を LECバックアップ_顧客在庫_日付.xlsx で書き出し(1793行)、1794行: localStorage.setItem('lec_last_backup',...)、1795行: EDIT_LOG記録 / 5255-5258行: roleOf(p.userName)==='admin' 限定の『💾 バックアップ』ボタン、overdue>=7日で btn-rd＋「（要）」、実行後トースト「…吉野さんへ送付してください。」 / 動作担保: 101行 XLSX CDN、2288行 roleOf定義、2246行 CUSTS、2446行 td2、3304行 h(PartsPage,{...,userName:userName[0],toast:showToast})、5226行 p.tab==='parts' で部材在庫一覧タブに露出 / 自動化不在の根拠: setIntervalは2442行 setInterval(saveAll,3000) のみ、resend/sendgrid/smtp/emailjs/mailto は全文ヒット0件、リポジトリに .yml/workflow/バックアップ用スクリプトなし、SUPABASE_SETUP.sql は lec_state テーブル作成のみ / 修正まとめ_20260616.html 151-155行: 顧客報告も手動ボタン＋リマインドとして説明、257行: 「② バックアップの『毎週自動送付』…自動メール送信ができず…メール送信基盤（追加設定）が必要」
**不足/差分**: (1) 毎週の自動スケジュール実行が未実装（手動ボタンのみ。静的サイト構成のため要外部基盤） (2) 吉野さんへの自動メール送付が未実装（トーストで手動送付を促すのみ。メール送信基盤の追加設定が必要と顧客報告257行に明記、顧客回答待ち） (3) 追加発見: リマインド基準日 lec_last_backup が端末ローカルlocalStorageのみ（2313行のLS_KEYSに含まれずSupabase lec_state同期対象外）のため、別端末・別管理者には実施済みでも赤「（要）」表示が残り、逆にキャッシュ消去で履歴も消える。週次実施の組織的担保としては不完全